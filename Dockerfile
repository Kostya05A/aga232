# Используем официальный образ Python
FROM python:3.9

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем ваш файл main.py внутрь контейнера
COPY main.py .

# Команда для запуска приложения
CMD ["python", "main.py"]
